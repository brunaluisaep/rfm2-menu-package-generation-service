# App acronym that will be prepended to new bootstrapped property sources that will map to env based parameters in AWS Parameter Store
spring.application.name=pkg
aws.paramstore.enabled=true
aws.paramstore.prefix=
aws.paramstore.profileSeparator=/
